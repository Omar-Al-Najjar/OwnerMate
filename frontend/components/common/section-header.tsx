type SectionHeaderProps = {
  eyebrow: string;
  title: string;
  description: string;
};

export function SectionHeader({
  eyebrow: _eyebrow,
  title: _title,
  description: _description,
}: SectionHeaderProps) {
  return null;
}
